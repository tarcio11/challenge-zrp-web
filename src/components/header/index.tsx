import { HeaderNav } from "./header-nav";

export const Header = () => {
  return (
    <header className="text-xs text-gray-900 uppercase dark:text-gray-400">
      <HeaderNav />
    </header>
  );
};
